using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// A controller specialized for Human players.
/// 
/// Currently a PlayerController does the same as a regular controller.
/// Later on this will change to handle input (the way human players differ
/// from AI players is that human players use mice, keyboards and joystics).
/// </summary>
public class PlayerController : Controller
{
}
